x=int(input("donner le valeur de x:"))
y=int(input("donner le valeur de y:"))
operation=(input("donner l'operation "))
    
if("operation==+"):
    (z=x+y)
if("operation==*"):
    (z=x*y)
    
print("le resultat est",z)
 
